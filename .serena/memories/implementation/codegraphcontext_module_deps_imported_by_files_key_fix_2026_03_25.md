Validated CodeGraphContext MCP in photo-finish and found module_deps failure in analyze_code_relationships: KeyError on results['imported_by_files']. Root cause in code_finder.py module_deps branch; find_module_dependencies returns keys {'module_name','importers','imports'}. Added regression test in cloned CodeGraphContext repo: tests/unit/core/test_code_finder_relationships.py::test_module_deps_summary_uses_importers_key. Patched source and local installed package to use len(results.get('importers', [])) for summary. Verified test red->green in clone with PYTHONPATH=src and verified installed package logic via Python snippet and fresh CLI process (no KeyError). Running MCP server process may need restart to load patched code.